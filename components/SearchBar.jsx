import styled from 'styled-components';
// Styled Components

const SearchBarInput = styled.input`
    width: auto;
    margin-bottom: 2px;
`;

function SearchBar() {
    return (
        <SearchBarInput />
    );
}

export default SearchBar;